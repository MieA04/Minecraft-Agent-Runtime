# Discoveries

Create one Markdown file per investigation. Copy `TEMPLATE.md`, name it `<YYYYMMDD>-<sequence>-<topic>.md`, and retain exact evidence needed to audit the conclusion.

Do not write unverified guesses into stable project, Minecraft, or Loader knowledge.
